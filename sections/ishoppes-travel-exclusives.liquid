{% schema %}
{
  "name": "Travel Exclusives",
  "tag": "section",
  "class": "travel-exclusives",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "TRAVEL EXCLUSIVES"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Product Card",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Travel Exclusives",
      "category": "Custom"
    }
  ]
}
{% endschema %}

{% style %}
  .travel-exclusives {
    padding: 10px 20px;
    background: #fff;
  }
  .travel-exclusives h2 {
    font-size: 28px;
    font-weight: 500;
    margin-bottom: 30px;
    text-transform: uppercase;
  }
  .product-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 30px;
    justify-content: center;
  }
  .card {
    background: #fff;
    width: 320px;
    position: relative;
    transition: 0.3s ease;

    padding: 15px 30px;
    box-shadow: 0 0 20px 0 rgba(6, 0, 0, 0.08);
    border-radius: 30px;
    min-height: 475px;
    padding-bottom: 90px;
  }
  .card:hover {
    transform: translateY(-5px);
  }
  .card a {
    text-decoration: none;
    color: inherit;
  }
  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
  }
  .vendor {
    font-weight: 600;
    font-size: 18px;
  }
  .product-details {
    display:flex;
    justify-content: space-between;
  }
  .subtitle {
    font-size: 14px;
    color: #555;
    margin-bottom: 6px;
  }
  .price {
    color: #007bff;
    font-weight: 500;
    font-size: 14px;
    margin-bottom: 15px;
  }
  .card img {
    display: block;
    max-width: 100%;
    max-height: 200px;
    height: 200px;
    width: 300px;
    min-height: 200px;
    -o-object-fit: contain;
    object-fit: contain;
    margin: 0 auto;
  }
  .arrow {
    font-size: 18px;
    color: #111;
  }
  .wishlist {
    position: absolute;
    bottom: 0px;
    right: 24px;
    font-size: 40px;
    color: #111;
    cursor: pointer;
  }
{% endstyle %}

<section class="travel-exclusives">
  <div class="page-width">
    <h2>{{ section.settings.heading }}</h2>
    <div class="product-grid">
      {% for block in section.blocks %}
        {% assign product = block.settings.product %}
        {% if product %}
          <div class="card">
            <a href="{{ product.url }}">
              <div class="card-header">
                <div class="vendor">{{ product.vendor }}</div>
                <div class="arrow">→</div>
              </div>
              <div class="product-details">
                <div class="subtitle">{{ product.title }}</div>
                <div class="price">{{ product.price | money }}</div>
              </div>
              {% if product.featured_image %}
                <img
                  src="{{ product.featured_image | image_url: width: 300 }}"
                  alt="{{ product.featured_image.alt | escape }}"
                  width="300"
                  height="{{ 300 | divided_by: product.featured_image.aspect_ratio | round }}"
                  loading="lazy"
                >
              {% endif %}
            </a>
            <div class="wishlist">♡</div>
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>
